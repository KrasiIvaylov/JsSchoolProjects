function solve() {

    const textArea = document.querySelectorAll('textarea');
    const buttons = document.querySelectorAll('button');

    buttons[0].addEventListener('click', function (e){
        const arr = JSON.parse(textArea[1].value);


    })

    buttons[0].addEventListener('click', function (e){

    })

}